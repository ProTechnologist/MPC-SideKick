<Window x:Class="MPC.SideKick.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="250" Width="400" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Media Player Path:" Margin="0,0,0,5"/>
        <DockPanel Grid.Row="1" Margin="0,0,0,15">
            <Button Content="..." Click="BrowseMpc_Click" DockPanel.Dock="Right" Width="30" Margin="5,0,0,0"/>
            <TextBox x:Name="MpcPathBox" Background="#33FFFFFF" Foreground="White" BorderThickness="1" BorderBrush="#66FFFFFF" Padding="5"/>
        </DockPanel>

        <CheckBox x:Name="RememberFolderCheck" Grid.Row="2" Content="Remember last open folder path" Foreground="White" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
